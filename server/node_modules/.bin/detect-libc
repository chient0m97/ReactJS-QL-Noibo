#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
<<<<<<< HEAD:server/node_modules/.bin/uid
  "$basedir/node"  "$basedir/../uid/bin/uid" "$@"
  ret=$?
else 
  node  "$basedir/../uid/bin/uid" "$@"
=======
  "$basedir/node"  "$basedir/../detect-libc/bin/detect-libc.js" "$@"
  ret=$?
else 
  node  "$basedir/../detect-libc/bin/detect-libc.js" "$@"
>>>>>>> origin/TrungN:server/node_modules/.bin/detect-libc
  ret=$?
fi
exit $ret
